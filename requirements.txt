pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.40
finance-datareader>=0.9.90
# NOTE: PyPI opendartreader requires Python >=3.13 and its wheels ship the
# package under the lowercase module name "opendartreader" (the GitHub source
# uses "OpenDartReader"); the code accepts either module name. On Python <3.13
# install from GitHub: pip install git+https://github.com/FinanceData/OpenDartReader
opendartreader>=0.2.3
anthropic>=0.40.0
python-dotenv>=1.0.1
lxml>=5.0.0
requests>=2.31.0
