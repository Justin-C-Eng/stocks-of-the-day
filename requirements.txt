pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.40
finance-datareader>=0.9.90
# NOTE: the opendartreader 0.2.3 sdist on PyPI declares python_requires>=3.13 and
# installs with a broken flat layout on older interpreters; on Python <3.13 install
# it from GitHub instead: pip install git+https://github.com/FinanceData/OpenDartReader
opendartreader>=0.2.3
anthropic>=0.40.0
python-dotenv>=1.0.1
lxml>=5.0.0
requests>=2.31.0
